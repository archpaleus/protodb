# Protobuf Query Tool

Tool for querying data from text-encoded and binary-encoded protobuf data.

## Functions


## Select based queries
```
queryproto select file.* from google.protobuf.FileDescirptorSet < ~/descriptor_set.binproto
```